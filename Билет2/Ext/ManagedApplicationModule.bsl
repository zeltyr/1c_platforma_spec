
Процедура ПриНачалеРаботыСистемы()
	
	ПодключитьОбработчикОжидания("ОткрытьФормыЕжедневныхЗаданий", 10);
	
КонецПроцедуры

Процедура ОткрытьФормыЕжедневныхЗаданий() Экспорт
	
	МасЗадач = РаботаСЗадачами.ПолучитьНеВыполненныеЗадания();
	Для каждого ЗадачаСсылка Из МасЗадач Цикл
	
		ПараметрыФормы = Новый Структура("Ключ", ЗадачаСсылка);
		ОткрытьФорму("Задача.Задача.ФормаОбъекта", ПараметрыФормы,Неопределено, ЗадачаСсылка.УникальныйИдентификатор());
		
	КонецЦикла;
	
	
	
КонецПроцедуры
