
&НаКлиенте
Процедура ПериодСПриИзменении(Элемент)
	
	Если ЗначениеЗаполнено(Объект.ПериодС) Тогда
		Объект.ПериодС = НачалоНедели(Объект.ПериодС);
		Объект.ПериодПо = КонецНедели(Объект.ПериодС);
	Иначе
		Объект.ПериодПо = Объект.ПериодС;
	КонецЕсли;
	
КонецПроцедуры
